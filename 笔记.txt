auto循环不能以指针为范围使用。二维数组行会自动转化为指针。
delete前先检查指针是否为野指针，若为野指针会报错
map底层为红黑树，性能最好。数组浪费空间（由于技术革新，现在的问题并非由于无法预知需要多少
个而容易多分配，而在于开辟(n,n)后横纵坐标小于n的空间也会全部开辟，而且空间单元大小一定不自
适应），链表浪费时间，vector功能多但性能差。
erase,insert函数时间为O(n)
字符串尽量不进map
字符串比较非常费时间，少用
指针常数较大
map会自动按大小排序，别乱用
map为唯一能自定义下标的c++容器
map不能列表赋值
map不存在的key对应值默认为0。直接输出就是0。但对应值为0的key不默认不存在。将键对应值减到0，size不减。将键erase,size-1.
C++编译器不支持负数求正余数，g++可以
map里相同的下标只能有一个元素，重新定义会直接覆盖。可以链表。
求余数要注意相等时余0.
文件需要在vscode终端运行的目录下才能直接打开，而不一定要在代码文件同一目录下。
找不到头文件就把bin加到环境变量里。搜索可能无法迭代进行。
map坑多，做题能用数组用数组
数组不能直接用数组名一下一起cout
题里给出的数据量范围可能不准确，数组要开大一点
