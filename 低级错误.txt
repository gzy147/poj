不小心把数据结构定义在循环外且未清洗数据结构
值传参地址会变化，不能再用原先的指针、迭代器寻找
||写成&&
else后没有括号
函数定义/声明时隔开括号内参数的不是分号，是逗号
任何情况下，数组都不能下标越界，即便用来判断也不可以
a函数调用b函数，则b要在a前声明一下。
传入函数的数组实参只有数组名没有下标边界
函数迭代时，其中一层返回某值不等于全部迭代结束。
排序后，后面的不一定比前面的大/小，还可能相等